import {ScrollView} from 'react-native';

const HomeScreen = () => {
  return (
    <ScrollView
      contentContainerStyle={{flex: 1, backgroundColor: 'white'}}></ScrollView>
  );
};

export default HomeScreen;
